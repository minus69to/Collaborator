version: '3.8'

services:
  collaborator:
    build: .
    ports:
      - "3000:3000"
    environment:
      # Add your environment variables here
      # They will be available at runtime
      - NODE_ENV=production
    # Uncomment to mount .env file if you prefer
    # env_file:
    #   - .env
    restart: unless-stopped

